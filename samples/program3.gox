func fact(n int) int {
    if n <= 1 {
        return 1;
    } else {
        return n * fact(n - 1);
    }
}

func main() int {
    var result int = fact(5);
    print result;  // Imprime 120
    return 0;
}
